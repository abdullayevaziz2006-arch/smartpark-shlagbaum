generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  id           String   @id @default(uuid())
  name         String
  username     String   @unique
  password     String
  role         String   @default("CASHIER") // ADMIN (Tadbirkor), CASHIER
  ownedLots    ParkingLot[] @relation("UserParkingLots")
  parkingLotId String?
  parkingLot   ParkingLot? @relation("CashierParkingLot", fields: [parkingLotId], references: [id])
  telegramChatId String?  @unique
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model ParkingLot {
  id             String   @id @default(uuid())
  name           String
  bankName       String?
  bankAccount    String?
  mfo            String?
  inn            String?
  telegramChatId String?
  ownerId        String?
  owner          User?    @relation("UserParkingLots", fields: [ownerId], references: [id])
  cashiers       User[]   @relation("CashierParkingLot")
  tariffs        Tariff[]
  sessions       ParkingSession[]
  devices        Device[]
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}

model Car {
  id            String   @id @default(uuid())
  plateNumber   String   @unique
  isSubscriber  Boolean  @default(false)
  subscriberEnd DateTime?
  isBlacklisted Boolean  @default(false)
  cardNo        String?
  sessions      ParkingSession[]
  logs          Log[]
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model Tariff {
  id          String   @id @default(uuid())
  name        String
  pricePerMin Float
  freeMinutes Int      @default(0)
  maxDaily    Float?
  lotId       String
  lot         ParkingLot @relation(fields: [lotId], references: [id])
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ParkingSession {
  id          String   @id @default(uuid())
  carId       String
  car         Car      @relation(fields: [carId], references: [id])
  lotId       String
  lot         ParkingLot @relation(fields: [lotId], references: [id])
  entryTime   DateTime @default(now())
  exitTime    DateTime?
  fee         Float    @default(0)
  status      String   @default("ACTIVE") // ACTIVE, COMPLETED, UNPAID
  payments    Payment[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Payment {
  id          String   @id @default(uuid())
  sessionId   String
  session     ParkingSession @relation(fields: [sessionId], references: [id])
  amount      Float
  method      String   // CASH, PAYME, CLICK
  status      String   @default("SUCCESS") // PENDING, SUCCESS, FAILED
  createdAt   DateTime @default(now())
}

model Log {
  id          String   @id @default(uuid())
  type        String   // ENTRY, EXIT, BLACKLIST_ATTEMPT, BARRIER_OPEN
  carId       String?
  car         Car?     @relation(fields: [carId], references: [id])
  description String?
  createdAt   DateTime @default(now())
}

model Device {
  id        String   @id @default(uuid())
  name      String
  ipAddress String
  port      Int      @default(80)
  username  String?
  password  String?
  model     String   @default("iDS-TCM203-A") // iDS-TCM203-A, DS-TCG205-E, DS-TCG406
  type      String   @default("ENTRY") // ENTRY, EXIT
  status    String   @default("ONLINE") // ONLINE, OFFLINE
  lotId     String
  lot       ParkingLot @relation(fields: [lotId], references: [id])
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
